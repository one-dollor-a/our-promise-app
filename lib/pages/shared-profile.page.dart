import 'package:flutter/material.dart';

class SharedProfilePage extends StatelessWidget {
  const SharedProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Shared Profile Page');
  }
}
